services:
  - type: web
    name: atm-banking
    env: node
    plan: free
    buildCommand: |
      npm ci || npm install
      pip3 install -r requirements.txt || true
    startCommand: npm start
    envVars:
      - key: CREDS
        sync: false
